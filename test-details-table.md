# פירוט בדיקות תוכנה

להלן טבלה המפרטת את בדיקות התוכנה שבוצעו במערכת, אופן ביצוען, התוצאות המתקבלות והתוצאות הרצויות:

| תיאור הבדיקה | אופן ביצוע | תוצאה רצויה | תוצאה מתקבלת |
|--------------|-----------|-------------|--------------|
| **בדיקת התחברות משתמש** | הרצת בדיקת `login` בקובץ `Login.vitest.test.jsx` | התחברות מוצלחת עם הפניה לדף הבית המתאים לסוג המשתמש | התחברות מוצלחת עם הפניה לדף הבית המתאים |
| **בדיקת התחברות עם פרטים שגויים** | הרצת בדיקת שגיאת התחברות ב-`Login.vitest.test.jsx` | הצגת הודעת שגיאה למשתמש | הצגת הודעת שגיאה למשתמש |
| **בדיקת הרשאות גישה** | הרצת בדיקת `PrivateRoute.vitest.test.jsx` | הפניה לדף התחברות כאשר המשתמש לא מחובר | הפניה לדף התחברות כאשר המשתמש לא מחובר |
| **בדיקת הרשאות לפי תפקיד** | הרצת בדיקת תפקידים ב-`PrivateRoute.vitest.test.jsx` | הפניה לדף הבית המתאים לתפקיד המשתמש | הפניה לדף הבית המתאים לתפקיד המשתמש |
| **בדיקת טעינת נתוני לוח בקרה לחברה** | הרצת בדיקת `Dashboard.vitest.test.jsx` לחברה | הצגת סטטיסטיקות ונתונים רלוונטיים לחברה | הצגת סטטיסטיקות ונתונים רלוונטיים לחברה |
| **בדיקת טעינת נתוני לוח בקרה לנהג** | הרצת בדיקת `Dashboard.vitest.test.jsx` לנהג | הצגת סטטיסטיקות ונתונים רלוונטיים לנהג | הצגת סטטיסטיקות ונתונים רלוונטיים לנהג |
| **בדיקת יצירת נסיעה** | הרצת בדיקת אינטגרציה `trip.test.js` | יצירת נסיעה חדשה במערכת | יצירת נסיעה חדשה במערכת |
| **בדיקת עדכון נסיעה** | הרצת בדיקת אינטגרציה `trip.test.js` | עדכון פרטי נסיעה קיימת | עדכון פרטי נסיעה קיימת |
| **בדיקת הקצאת נהג לנסיעה** | הרצת בדיקת אינטגרציה `trip.test.js` | הקצאת נהג לנסיעה ועדכון סטטוס הנסיעה | הקצאת נהג לנסיעה ועדכון סטטוס הנסיעה |
| **בדיקת התחלת נסיעה** | הרצת בדיקת אינטגרציה `trip.test.js` | עדכון סטטוס הנסיעה ל-"פעילה" | עדכון סטטוס הנסיעה ל-"פעילה" |
| **בדיקת סיום נסיעה** | הרצת בדיקת אינטגרציה `trip.test.js` | עדכון סטטוס הנסיעה ל-"הושלמה" | עדכון סטטוס הנסיעה ל-"הושלמה" |
| **בדיקת מודל Trip** | הרצת בדיקות יחידה למודל Trip | יצירה, עדכון ומחיקה של נסיעות בבסיס הנתונים | יצירה, עדכון ומחיקה של נסיעות בבסיס הנתונים |
| **בדיקת בקר Trip** | הרצת בדיקות יחידה לבקר Trip | טיפול נכון בבקשות API לניהול נסיעות | טיפול נכון בבקשות API לניהול נסיעות |
| **בדיקת נתיבי API לנסיעות** | הרצת בדיקות נתיבים לנסיעות | תגובות API תקינות עם קודי סטטוס מתאימים | תגובות API תקינות עם קודי סטטוס מתאימים |
| **בדיקת אימות משתמש** | הרצת בדיקת אינטגרציה `auth.test.js` | אימות משתמש והנפקת טוקן JWT | אימות משתמש והנפקת טוקן JWT |
| **בדיקת הגנת נתיבים** | הרצת בדיקת אינטגרציה `auth.test.js` | דחיית גישה לנתיבים מוגנים ללא טוקן תקף | דחיית גישה לנתיבים מוגנים ללא טוקן תקף |
| **בדיקת הרשאות לפי תפקיד בשרת** | הרצת בדיקת אינטגרציה `auth.test.js` | דחיית גישה לנתיבים שאינם מתאימים לתפקיד המשתמש | דחיית גישה לנתיבים שאינם מתאימים לתפקיד המשתמש |
| **בדיקת פונקציות עזר** | הרצת בדיקות `vitestUtils.vitest.test.jsx` | פעולה תקינה של פונקציות עזר | פעולה תקינה של פונקציות עזר |
| **בדיקת תצוגת כותרת** | הרצת בדיקות `Header.vitest.test.jsx` | הצגת תפריט ניווט מותאם לתפקיד המשתמש | הצגת תפריט ניווט מותאם לתפקיד המשתמש |
| **בדיקת התראות בזמן אמת** | הרצת בדיקות לרכיב התראות | הצגת התראות חדשות למשתמש | הצגת התראות חדשות למשתמש |

כל הבדיקות הנ"ל עברו בהצלחה, כפי שניתן לראות בדוחות הכיסוי שנוצרו בתיקיית `test-reports`. הבדיקות מכסות את כל השכבות של המערכת: מודלים, בקרים, נתיבי API, רכיבי ממשק משתמש, ספקי הקשר ופונקציות עזר.
